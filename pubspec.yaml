name: appointment_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  
  # Dependencias de Firebase
  firebase_core: ^2.24.0
  firebase_auth: ^4.17.3
  cloud_firestore: ^4.13.0
  
  # Dependencias de UI
  cached_network_image: ^3.3.0
  flutter_bloc: ^9.1.1
  bloc: ^9.1.0
  intl: ^0.20.2  # ← CAMBIA ESTO A 0.20.2
  cupertino_icons: ^1.0.8
  fl_chart: ^0.66.0
  
  # Esto está bien
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  # Quita 'generate: true' por ahora para simplificar